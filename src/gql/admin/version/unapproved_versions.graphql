query GetUnapprovedVersions($filter: VersionFilter) {
  getUnapprovedVersions(filter: $filter) {
    versions {
      id
      mod_id
      version
      stability
      link
      arch {
        platform
        asset
      }
      changelog
      created_at

      mod {
        name
      }
    }
    count
  }
}
