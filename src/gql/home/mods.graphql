query GetMods($offset: Int!, $limit: Int!, $search: String) {
  getMods(filter: { limit: $limit, offset: $offset, search: $search }) {
    count
    mods {
      id
      name
      logo
      views
      downloads
      short_description
    }
  }
}
